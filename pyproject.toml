[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "g97-search"
version = "0.1.0"
description = "Historically constrained classical web-search research reference engine"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
g97 = "g97.cli:main"

[tool.setuptools]
packages = ["g97"]

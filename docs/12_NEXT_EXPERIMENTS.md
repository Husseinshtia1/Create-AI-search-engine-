# الاختبارات التالية

## E1 Curlie v8 external validation
لا تغيير في protocol؛ feasibility أولًا، ثم frozen controller مرة واحدة، مع Body@30/ForcedHybrid/v8/gate/wins-losses/bootstrap، وإظهار multilingual limitation بلا post-hoc fix.

## E2 Lexical Foundation
تعلم selection/controller على dev collections ثم اختبار untouched textual corpus؛ لا اختيار بعد رؤية qrels.

## E3 Third real graph+qrels IR corpus
query judgments حقيقية + native relations للتحقق بعيدًا عن classification stress.

## E4 WT2g
Frozen lexical baselines + global-link control + query-local anchor/host/context + separate external-description channel + no tuning after qrels.

## E5 Multi-action Intervention Controller
Actions متعددة بتكاليف مختلفة؛ expected utility لا accuracy فقط؛ validation عبر datasets مختلفة.

## E6 Robustness
link farms، keyword stuffing، sitewide navigation، duplicates، new high-quality pages بلا links، anchor poisoning.

## E7 Freshness real trace
crawl snapshots زمنية حقيقية لTTQ/staleness بدل synthetic oracle.

Success: external web validation، real web IR transfer، spam robustness، independent reproduction، prior-art boundary واضحة.
